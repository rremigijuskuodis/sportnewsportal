import { AdminSetup } from "@/components/admin-setup";

export const metadata = { title: "Administratoriaus paskyros aktyvavimas", robots: { index: false, follow: false } };

export default function Page() { return <AdminSetup />; }
